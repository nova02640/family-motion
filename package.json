{
  "name": "mir-web",
  "version": "0.1.0",
  "private": true,
  "description": "Web-based Legend (Mir2) style MMORPG - Phaser 4 + Colyseus",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm -w server run dev",
    "dev:client": "npm -w client run dev",
    "build": "npm -w shared run build && npm -w server run build && npm -w client run build",
    "typecheck": "npm -w shared run typecheck && npm -w server run typecheck && npm -w client run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
